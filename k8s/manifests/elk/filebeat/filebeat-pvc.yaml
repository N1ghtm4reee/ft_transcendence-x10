apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elk-pvc
  namespace: elk
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: elk-pv
  storageClassName: ""